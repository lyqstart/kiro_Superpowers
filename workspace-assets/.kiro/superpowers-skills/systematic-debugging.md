# systematic-debugging (Kiro Skill Card)

Use for bugs/failures. Reproduce, trace root cause, compare working vs broken examples, validate hypotheses one at a time, apply defense-in-depth, avoid fixed sleeps, stop after repeated failed fixes.
