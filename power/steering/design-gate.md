# Design Gate

## 进入 tasks 前必须检查

设计必须覆盖：

- 架构边界；
- 组件职责；
- 数据流；
- API / 接口；
- 数据模型；
- 错误处理；
- 权限与安全；
- 测试策略；
- 对现有功能的影响；
- 不做什么。

## 设计审查问题

在生成 tasks 前，回答：

1. 每个 requirement 是否有设计落点？
2. 每个设计模块是否有清晰责任？
3. 是否引入不必要复杂度？
4. 是否有难以测试的设计？
5. 是否会破坏已有行为？
6. 是否需要迁移、兼容或回滚方案？

不通过则先 refine design，不要进入实现。
