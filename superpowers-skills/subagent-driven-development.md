# subagent-driven-development (Kiro Skill Card)

Use for Kiro task execution with subagents. Main agent creates Subagent Task Packet, dispatches fresh role-specific subagent where possible, verifies result, runs spec review then code review, and records evidence.
