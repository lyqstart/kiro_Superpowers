# using-git-worktrees (Kiro Skill Card)

Use before implementation. Prefer isolated worktree, inspect clean git state, choose directory safely, run baseline verification, write metadata, avoid destructive cleanup without confirmation.
